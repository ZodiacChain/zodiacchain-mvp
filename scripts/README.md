# Scripts

Local and testnet helper scripts for the ZodiacChain MVP.

Possible responsibilities:

- local environment setup;
- contract deployment helpers;
- mock data generation;
- testnet interaction helpers;
- demo preparation tasks.

Scripts must not contain secrets or private credentials.